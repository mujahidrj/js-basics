// Function that determines if a picture with given dimensions
// is in portrait mode or landscape mode
console.log(isPortrait(100,20));

function isPortrait(height, width) {
    return height > width;
}